@model CategoryListViewModel
@{
    ViewData["Title"] = "Kategorie";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Kategorie</h1>
    <a asp-action="CreateCategory" class="btn btn-primary">+ Dodaj kategorię</a>
</div>

@if (!Model.Categories.Any())
{
    <div class="alert alert-info">Brak kategorii.</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped align-middle">
            <thead>
                <tr>
                    <th>Nazwa</th>
                    <th>Opis</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model.Categories)
                {
                    <tr>
                        <td>@category.Name</td>
                        <td>@category.Description</td>
                        <td class="text-end">
                            <a asp-action="EditCategory" asp-route-id="@category.Id" class="btn btn-sm btn-outline-secondary">Edytuj</a>
                            <form asp-action="DeleteCategory" asp-route-id="@category.Id" method="post" class="d-inline"
                                  onsubmit="return confirm('Usunąć kategorię @category.Name?');">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-danger">Usuń</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}

<p class="mt-3">
    <a asp-action="Index" class="btn btn-outline-secondary">← Panel admina</a>
</p>
